[build]
  command = "npm run build"
  publish = "dist/open/browser"
  functions = "netlify/functions"

# ponytail: force ALL requests through SSR function, no static fallback
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/ssr"
  status = 200
  force = true
